name: Deploy to Fly.io

env:
  FLY_APP: web-fzmoua

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: fly-deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy polidog.jp
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment:
      name: production
      url: https://polidog.jp

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Check Fly app access
        run: flyctl status -a "$FLY_APP"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy
        run: flyctl deploy --remote-only -a "$FLY_APP"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
